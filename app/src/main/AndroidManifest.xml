<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mediaapp">
    <uses-permission android:name="android.permission.INTERNET" />  //access to use the internet in the users coice
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MediaApp"
        android:usesCleartextTraffic="true">
        <activity android:name=".main2"
            android:label="LIST"
            android:parentActivityName=".MainActivity">

        </activity>
        <activity android:name=".main3"
            android:label="SONG 2"
            android:parentActivityName=".main2">
        </activity>
        <activity android:name=".main5"
            android:label="SONG 3"
            android:parentActivityName=".main2">
        </activity>


        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>